# API Configuration
# For local development, use: http://localhost:5000
# For production, use your backend URL (e.g., https://api.khoablacktopup.vn)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Socket.IO Configuration (Optional)
# By default, Socket.IO will use NEXT_PUBLIC_API_URL
# Only set this if Socket.IO runs on a different URL than the API
# NEXT_PUBLIC_SOCKET_URL=http://localhost:5000

# App URL (for SEO and meta tags)
# For local development, use: http://localhost:3000
# For production, use your frontend URL (e.g., https://khoablacktopup.vn)
NEXT_PUBLIC_APP_URL=http://localhost:3000
