# ==================================================
# KhoaBlack Frontend Environment Variables
# ==================================================

# ===== API Configuration =====
# For local development, use: http://localhost:5000
# For production with Nginx proxy: https://khoablacktopup.vn/api
# QUAN TRỌNG: Đảm bảo URL này khớp với backend CORS_ORIGINS!
NEXT_PUBLIC_API_URL=http://localhost:5000

# ===== Socket.IO Configuration =====
# QUAN TRỌNG: Socket.IO trong production với Nginx proxy
#
# Development (trực tiếp vào backend socket port):
#   NEXT_PUBLIC_SOCKET_URL=http://localhost:5001
#
# Production (qua Nginx proxy tại /socket.io/):
#   NEXT_PUBLIC_SOCKET_URL=https://khoablacktopup.vn
#   Nginx sẽ proxy /socket.io/ -> http://127.0.0.1:5001
#
# Nếu không set, sẽ fallback về NEXT_PUBLIC_API_URL hoặc window.location.origin
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001

# ===== App URL (for SEO and meta tags) =====
# For local development: http://localhost:3000
# For production: https://khoablacktopup.vn
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===== Internal API (Optional - for SSR calls) =====
# Use for faster internal network calls in Docker/VPS
# INTERNAL_API_URL=http://127.0.0.1:5000
